var numSquares = function(n) {
// perfect square is an integer that is the square of an integer; in other words, it is the product of some integer with itself.

/*
  - create a list of perfect squares from 1 upto and including `n`
  - get largestPS which is less than or equal to `n`
    - starting from max PS left of largestPS



  [1, 4, 9, 16, ...] -> 12

*/
};